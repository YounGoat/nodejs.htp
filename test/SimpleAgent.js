'use strict';

const MODULE_REQUIRE = 1
    /* built-in */
    
    /* NPM */
    
    /* in-package */
    , SimpleAgent = require('../SimpleAgent')
    ;

describe('SimpleAgent', () => {
   
    it('*', () => {
       let agent = new SimpleAgent();
    });
    
});